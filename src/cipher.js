function cifrado ()
const.textoIngresado = document.getElementById("texto").value;
textoCifrado.value = textoIngresado.split("").map(c>){;
let numeroEntero = "ABC".charCodeAt(0);
let mayus = (c === c.toUpperCase()) ? true : false;
if(valorEntero >= 97 && valorEntero <= 122)
			{



function copiarDatos(){   var textoIngresado = document.getElementById("texto").value;
  let textoCifrado = document.getElementById("formulario").value;

  let textoCifrado = textoIngresado;

  document.getElementById("texto").innerHTML = texto;
